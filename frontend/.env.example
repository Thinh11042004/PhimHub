# Example environment variables for frontend
# Backend API base URL (include /api suffix). Default dev: http://localhost:3001/api
VITE_API_BASE_URL=

# Optional dev JWT for local testing (used by TokenSetter if present)
VITE_DEV_JWT=
